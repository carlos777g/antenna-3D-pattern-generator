# Schema (shared data contract)

Optional package, not yet confirmed. If added, this package holds the
shared TypeScript/JSON Schema definitions for the unified data contract
(see docs/data-schema.md), imported by both packages/client and
packages/server to prevent silent schema drift.
