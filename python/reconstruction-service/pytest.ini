[pytest]
# The service root and tests/ are both on sys.path: modules are imported as
# `modules.x` / `config.x` (matching pipeline.py), and conftest helpers are
# imported directly by the test modules.
pythonpath = . tests
testpaths = tests
